spring:
  application:
    name: frellas

  datasource:
    url: jdbc:postgresql://localhost:5440/frellas
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true